import { IsHomeActivePipe } from './is-home-active.pipe';

describe('IsHomeActivePipe', () => {
  it('create an instance', () => {
    const pipe = new IsHomeActivePipe();
    expect(pipe).toBeTruthy();
  });
});
